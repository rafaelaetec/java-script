function verificar(){
    var piano = window.document.getElementsByName ('tecla')
    var local = window.document.getElementsByName ('local')
    var tipo = ''
    if(piano == tecla[0] && local == local[0]){
        
    }
}